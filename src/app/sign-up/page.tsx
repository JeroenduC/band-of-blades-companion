import { SignUpForm } from '@/components/features/auth/sign-up-form';

export const metadata = { title: 'Create account — Band of Blades' };

export default function SignUpPage() {
  return <SignUpForm />;
}
