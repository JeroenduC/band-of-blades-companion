import { SignInForm } from '@/components/features/auth/sign-in-form';

export const metadata = { title: 'Sign in — Band of Blades' };

export default function SignInPage() {
  return <SignInForm />;
}
